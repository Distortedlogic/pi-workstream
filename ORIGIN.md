# Source origin

The initial coordinator and tests were copied from `~/forked/pi-workstream`.

This is a separate repository. Its implementation uses public extension events, not source imports from the original project. The original MIT license remains in `LICENSE`.
